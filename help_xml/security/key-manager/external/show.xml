<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/keymanager/external/keymanager_external.smf
    table name: keymanager_external
-->
<command>
    <name>security key-manager external show</name>
    <description>This command displays the external key management servers configured
    on the cluster for a given Vserver. No entries are displayed when external
    key management is not enabled for the given Vserver. This command displays
    the primary external key management servers, along with any associated
    secondary key servers, configured on the cluster for a given Vserver.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers for the given Vserver.</description>
        </parameter>
        <parameter>
            <name>key-server</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers with the given primary key server host name or IP address listening on the given port.
            </description>
        </parameter>
        <parameter>
            <name>client-cert</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers using a client certificate with the given name.</description>
        </parameter>
        <parameter>
            <name>server-ca-certs</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers using server-ca certificates with the given names.</description>
        </parameter>
        <parameter>
            <name>timeout</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers using the given I/O timeout.</description>
        </parameter>
        <parameter>
            <name>username</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers using the given authentication username.</description>
        </parameter>
        <parameter>
            <name>policy</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers using the given key manager policy.</description>
        </parameter>
        <parameter>
            <name>secondary-key-servers</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers with the given secondary key servers.</description>
        </parameter>
        <parameter>
            <name>create-remove-timeout</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers using the given create-remove I/O timeout.</description>
        </parameter>
        <parameter>
            <name>enabled</name>
            <description>If you specify this parameter, then the command displays only the key management
            servers that are enabled.</description>
        </parameter>
    </parameters>
    <example>
        <p>The following example lists all configured key management servers for all Vservers:</p>
        <screen>
cluster-1::> security key-manager external show

                  Vserver: datavs
       Client Certificate: datavsClientCert
   Server CA Certificates: datavsServerCaCert1, datavsServerCaCert2
          Security Policy: IBM_Key_Lore
                  Enabled: true

Primary Key Server
----------------------------------------------------------------------
keyserver.datavs.com:5696

                  Vserver: cluster-1
       Client Certificate: AdminClientCert
   Server CA Certificates: AdminServerCaCert
          Security Policy:
                  Enabled: true


Primary Key Server
----------------------------------------------------------------------
10.0.0.10:1234
    Secondary Servers: ks1.local, ks2.local
fd20:8b1e:b255:814e:32bd:f35c:832c:5a09:1234
ks1.local:1234
4 entries were displayed.

        </screen>
        <p>The following example lists all configured key management servers with more detail,
        including timeouts and usernames:</p>
        <screen>
cluster-1::> security key-manager external show -instance

                  Vserver: datavs
       Client Certificate: datavsClientCert
   Server CA Certificates: datavsServerCaCert1, datavsServerCaCert2
       Primary Key Server: keyserver.datavs.com:5696
                  Timeout: 25
                 Username: datavsuser
          Security Policy: IBM_Key_Lore
                  Enabled: true
    Secondary Key Servers:

                  Vserver: cluster-1
       Client Certificate: AdminClientCert
   Server CA Certificates: AdminServerCaCert
       Primary Key Server: 10.0.0.10:1234
                  Timeout: 25
                 Username:
          Security Policy:
                  Enabled: true
    Secondary Key Servers: ks1.local, ks2.local

                  Vserver: cluster-1
       Client Certificate: AdminClientCert
   Server CA Certificates: AdminServerCaCert
       Primary Key Server: fd20:8b1e:b255:814e:32bd:f35c:832c:5a09:1234
                  Timeout: 25
                 Username:
          Security Policy:
                  Enabled: true
    Secondary Key Servers:

                  Vserver: cluster-1
       Client Certificate: AdminClientCert
   Server CA Certificates: AdminServerCaCert
       Primary Key Server: ks1.local:1234
                  Timeout: 45
                 Username:
          Security Policy:
                  Enabled: true
    Secondary Key Servers:
4 entries were displayed.

        </screen>
    </example>
</command>
