<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/keymanager/external/keymanager_external.smf
    table name: keymanager_external
-->
<command>
    <name>security key-manager external enable</name>
    <description>This command enables the external key manager associated with
    the given Vserver. This command is not supported when a key manager for
    the given Vserver is already enabled. When enabling the external key manager
    associated with the admin Vserver, you must run the same command specifying
    the same set of key servers on the peer cluster. When enabling the external
    key manager for a data Vserver, you can run the
    <cmdname>security key-manager external enable</cmdname> command on the active
    cluster only, as the configuration will be replicated on the peer cluster.
    However, you must ensure that the key management servers specified in the
    <cmdname>security key-manager external enable</cmdname> command are reachable
    from both clusters. Only primary key servers can be added using this command.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
            <description>Use this parameter to specify the Vserver on which the
            external key manager is to be enabled.</description>
        </parameter>
        <parameter>
            <name>key-servers</name>
            <description>Use this parameter to specify the list of up to four
            key management servers that the external key manager uses to
            store keys.
            </description>
        </parameter>
        <parameter>
            <name>client-cert</name>
            <description>Use this parameter to specify the unique name of the
            client certificate that the key management servers use to
            ensure the identity of ONTAP.</description>
        </parameter>
        <parameter>
            <name>server-ca-certs</name>
            <description>Use this parameter to specify the unique names of
            server-ca certificates that ONTAP uses to ensure the
            identify of the key management servers.</description>
        </parameter>
        <parameter>
            <name>policy</name>
            <description>Use this parameter to specify a specific key manager
            security policy to be used by this key manager.</description>
        </parameter>
    </parameters>
    <example>
        <p>The following example enables the external key manager for Vserver
        cluster-1.  The command includes three key management servers. The first
        key server's hostname is ks1.local and is listening on port 15696. The
        second key server's IP address is 10.0.0.10 and is listening on the
        default port 5696.  The third key server's IPv6 address is
        fd20:8b1e:b255:814e:32bd:f35c:832c:5a09, and is listening on port 1234.
        </p>
        <screen>
cluster-1::> security key-manager external enable -vserver cluster-1 -key-servers ks1.local:15696,10.0.0.10,[fd20:8b1e:b255:814e:32bd:f35c:832c:5a09]:1234 -client-cert AdminVserverClientCert -server-ca-certs ServerCaCert1,ServerCaCert2
        </screen>
    </example>
</command>
