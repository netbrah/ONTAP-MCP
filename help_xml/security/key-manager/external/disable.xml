<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/keymanager/external/keymanager_external_disable.smf
    table name: keymanager_external_disable
-->
<command>
    <name>security key-manager external disable</name>
    <description>This command disables the external key manager associated
    with the given Vserver. If the key manager is in use by ONTAP, you cannot
    disable it. When disabling the external key manager associated with
    the admin Vserver, you must run the same command on the peer cluster.
    When disabling the external key manager for a data Vserver, you can run
    the <cmdname>security key-manager external disable</cmdname> command on
    the active cluster only, as the command is replicated on the peer cluster.
    This command is not supported when the Onboard Key Manager is
    enabled for the given Vserver.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
            <description>Use this parameter to specify the Vserver on which the
            external key manager is to be disabled.</description>
        </parameter>
	</parameters>
	<example>
		<p>The following example removes the external key manager for Vserver
		cluster-1:</p>
		<screen>
cluster-1::*> security key-manager external disable -vserver cluster-1

 Warning: This command will permanently delete the external key management
 configuration for Vserver "cluster-1".
 Do you want to continue? {y|n}: y
		</screen>
	</example>
</command>
