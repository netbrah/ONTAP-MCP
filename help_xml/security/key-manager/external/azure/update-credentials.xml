<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/external/azure/azure_key_vault_ui.smf
    table name: azure_key_vault_ui
-->
<command>
    <name>security key-manager external azure update-credentials</name>
    <description>This command provides a way to update the authentication credentials that are
    used for an Azure Key Vault (AKV) configuration. The -config-name parameter can optionally be set
    to specify a specific AKV configuration of the given Vserver. If the -config-name parameter is not set,
    the credentials of the given Vserver's enabled configuration are updated. The credentials are
    initially set by running either the <cmdname>security key-manager external azure enable</cmdname> or
    <cmdname>security key-manager external azure create-config</cmdname> command.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
            <description>Use this parameter to specify the Vserver for which the
            AKV client credentials are to be updated.</description>
        </parameter>
        <parameter>
            <name>config-name</name>
            <description>Use this parameter to specify the configuration name for which the
            AKV client credentials are to be updated.</description>
        </parameter>
        <parameter>
            <name>authentication-method</name>
            <description>Use this parameter to specify the authentication method.</description>
        </parameter>
        <parameter>
            <name>client-secret</name>
            <hide/>
        </parameter>
        <parameter>
            <name>client-certificate</name>
            <hide/>
        </parameter>
    </parameters>
    <example>
        <p>The following examples show different ways of updating the AKV client credentials for the data Vserver v1.
        </p>
        <screen>
cluster-1::> security key-manager external azure update-credentials -vserver v1 -authentication-method client_secret

Enter new client secret:

Re-enter new client secret:

cluster-1:> security key-manager external azure update-credentials -vserver v1 -authentication-method certificate

Enter the client certificate for Azure Key Vault:

cluster-1:> security key-manager external azure update-credentials -vserver v1 -config-name config10 -authentication-method certificate

Enter the client certificate for Azure Key Vault:

        </screen>
    </example>
</command>
