<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/external/azure/azure_key_vault_ui.smf
    table name: azure_key_vault_ui
-->
<command>
    <name>security key-manager external azure restore</name>
    <description>This command retrieves and restores any current unrestored keys associated with the
     given Vserver to the nodes internal key tables. This command is not supported when AKV has not
     been enabled for the Vserver.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
            <description>This parameter specifies the Vserver to which the missing keys will be
            restored.</description>
        </parameter>
    </parameters>
    <example>
        <p>The following command restores missing keys for the data vserver v1 (which has AKV
           configuration) to the internal key tables on the cluster.
        </p>
        <screen>
cluster-1::> security key-manager external azure restore -vserver v1

        </screen>
    </example>
</command>
