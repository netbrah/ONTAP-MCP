<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/external/azure/azure_key_vault_ui.smf
    table name: azure_key_vault_ui
-->
<command>
    <name>security key-manager external azure rekey-external</name>
    <description>This command results in the key hierarchy being protected by the user designated
    AKV key encryption key (KEK). Prior to running this command, the user should have already
    made the necessary change on the Azure portal to use a new KEK for their key vault.
    The key-id used in this command is the key ID associated with the user's new AKV KEK.
    Upon successful completion of this command, the internal keys for the given Vserver will be
    protected by the new AKV KEK.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
            <description>This parameter specifies the Vserver for which ONTAP should rekey the AKV KEK.</description>
        </parameter>
        <parameter>
            <name>key-id</name>
            <description>This parameter specifies the key id of the new AKV KEK that should
             be used by ONTAP for the provided Vserver.</description>
        </parameter>
    </parameters>
    <example>
        <p>The following command rekeys AKV KEK for data Vserver v1 using a new key, key2 with version 12345678123412341234123456789012.
        </p>
        <screen>
cluster-1::> security key-manager external azure rekey-external -vserver v1 -key-id  https://kmip-akv-keyvault.vault.azure.net/keys/key2/12345678123412341234123456789012

        </screen>
    </example>
</command>
