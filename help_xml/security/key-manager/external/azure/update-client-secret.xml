<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/external/azure/azure_key_vault_ui.smf
    table name: azure_key_vault_ui
-->
<command>
    <name>security key-manager external azure update-client-secret</name>
    <description><note>This command is deprecated and may be removed in a future release.
    Use <cmdname>security key-manager external azure update-credentials</cmdname> instead.</note>
    This command provides a way to update the client secret that is
    used for the Azure Key Vault (AKV) configured for the given Vserver. The command is
    initially set by running the <cmdname>security key-manager external azure enable</cmdname>
    command. This command is not supported if AKV has not been enabled for the
    Vserver.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
	    <description>Use this parameter to specify the Vserver for which the
            AKV client secret is to be updated.</description>
        </parameter>
        <parameter>
            <name>new-client-secret</name>
            <hide/>
        </parameter>
    </parameters>
    <example>
        <p>The following example updates the AKV client secret for the data Vserver v1.
        </p>
        <screen>
cluster-1::> security key-manager external azure update-client-secret -vserver v1

Enter new client secret:

Re-enter new client secret:

        </screen>
    </example>
</command>
