<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/external/gcp/gcp_kms_ui.smf
    table name: gcp_kms_ui
-->
<command>
    <name>security key-manager external gcp restore</name>
    <description>This command retrieves and restores any unrestored keys associated with the
     given Vserver to each node's internal key tables.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
            <description>This parameter specifies the Vserver to which the missing keys will be
            restored.</description>
        </parameter>
    </parameters>
    <example>
        <p>The following command restores missing keys for the data Vserver v1 (which has GCKMS
           enabled) to the internal key tables on each node in the cluster.
        </p>
        <screen>
cluster-1::> security key-manager external gcp restore -vserver v1

        </screen>
    </example>
</command>


