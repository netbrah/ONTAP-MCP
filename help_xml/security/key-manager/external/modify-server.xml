<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/keymanager/external/keymanager_external_modify_server.smf
    table name: keymanager_external_modify_server
-->
<command>
    <name>security key-manager external modify-server</name>
    <description>This command modifies configuration information for configured
    key management servers. When modifying a key management server from the
    external key manager associated with the admin Vserver, you must run the
    same command specifying the same set of parameters on the peer cluster.
    When modifying a key management server from a data Vserver, you can run the
    <cmdname>security key-manager external modify-server</cmdname> command on
    the active cluster only as the command is replicated on the peer cluster.
    However, if the password associated with a key management server is modified,
    then you must run the <cmdname>security key-manager external modify-server</cmdname>
    command specifying the same password on the peer cluster as the password is not
    replicated between clusters. This command is supported only when external key
    manager has been enabled for the given Vserver.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
            <description>Use this parameter to specify the Vserver on which to
            modify the key management server configuration.</description>
        </parameter>
        <parameter>
            <name>key-server</name>
            <description>Use this parameter to specify the primary key
            management server for which the command modifies the
            configuration.</description>
        </parameter>
        <parameter>
            <name>secondary-key-servers</name>
            <description>Use this parameter to specify the secondary key
            management servers that will be members of the set of clustered key
            servers. When specifying a secondary key server, a port number
            cannot be associated with the secondary key server.</description>
        </parameter>
        <parameter>
            <name>timeout</name>
            <description>Use this parameter to specify the I/O timeout, in
            seconds, for the selected key management server.</description>
        </parameter>
        <parameter>
            <name>username</name>
            <description>Use this parameter to specify the username with which
            ONTAP authenticates with the key management server.
            </description>
        </parameter>
        <parameter>
            <name>create-remove-timeout</name>
            <description>Use this parameter to specify a shorter I/O timeout, in
            seconds, to be used for create and delete operations for the selected key management server.</description>
        </parameter>
        <parameter>
            <name>port</name>
            <description>Use this parameter to specify the port number to be used for communicating with the selected key management server.</description>
        </parameter>
    </parameters>
    <example>
        <p>The following example modifies the I/O timeout to 45 seconds for
        Vserver cluster-1, key server keyserver1.local:</p>
        <screen>
cluster-1::> security key-manager external modify-server -vserver cluster-1 -key-server keyserver1.local -timeout 45
        </screen>
        <p>The following example modifies the username and passphrase used to
        authenticate with key server keyserver1.local:</p>
        <screen>
cluster-1::> security key-manager external modify-server -vserver cluster-1 -key-server keyserver1.local -username ksuser
Enter the password:
Reenter the password:
        </screen>
        <p>The following example modifies the secondary key management servers
        secondarykeyserver1.local and secondarykeyserver2.local to be in a
        cluster configuration with the primary key management server
        keyserver1.local</p>
        <screen>
cluster-1::> security key-manager external modify-server -vserver cluster-1 -key-server keyserver1.local -secondary-key-servers secondarykeyserver1.local,secondarykeyserver2.local
        </screen>
    </example>
</command>
