<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/external/aws/aws_kms_ui.smf
    table name: aws_kms_ui
-->
<command>
    <name>security key-manager external aws disable</name>
    <description>This command disables the Amazon Web Service Key Management Service (AWSKMS) associated
    with the given Vserver. AWSKMS cannot be disabled if it is in use by ONTAP.
    This command will fail if AWSKMS has not been enabled for the Vserver.
    </description>
    <parameters>
        <parameter>
            <name>vserver</name>
            <description>Use this parameter to specify the Vserver on which the
            AWSKMS is to be disabled.</description>
        </parameter>
    </parameters>
    <example>
        <p>The following example disables the AWSKMS for Vserver v1.
        </p>
        <screen>
cluster-1::>security key-manager external aws disable -vserver v1

        </screen>
    </example>
</command>
