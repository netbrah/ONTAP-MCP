<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/keymanager/onboard/keymanager_onboard_create_config.smf
    table name: keymanager_onboard_create_config
-->
<command>
    <name>security key-manager onboard create-config</name>
    <description>This command enables an ONTAP administrator to create an inactive configuration for the named Vserver. This facilitates the switching between the Onboard Key Manager (OKM) and an external key manager (EKM) without migrating the keys to a data SVM.</description>
    <example>
        <p>The following example creates an inactive configuration for the Onboard Key Manager:</p>
        <screen>
            cluster-1::*> security key-manager onboard create-config
        </screen>
    </example>
</command>
