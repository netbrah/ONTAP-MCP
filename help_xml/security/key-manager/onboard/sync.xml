<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/keymanager/onboard/keymanager_onboard_sync.smf
    table name: keymanager_onboard_sync
-->
<command>
  <name>security key-manager onboard sync</name>
  <description>This command synchronizes missing onboard keys on any node in the cluster.
  For example, if you add a node to a cluster that has the Onboard Key Manager configured,
  you should then run this command to synchronize the keys.
  In a MetroCluster configuration, if the <cmdname>security key-manager onboard enable</cmdname>
  command is used to enable the Onboard Key Manager on one site, then run the <cmdname>security
  key-manager onboard sync</cmdname> command on the partner site.
  In a MetroCluster configuration, if the <cmdname>security key-manager onboard
  update-passphrase</cmdname> command is used to update the passphrase on one site, then run
  this command with the new passphrase on the partner site before proceeding with any
  key management operations.
  </description>
  <example>
    <p>The following example synchronizes the Onboard Key Manager key database across all nodes in the cluster. In a MetroCluster configuration,
    this command synchronizes nodes in the local site.</p>
    <screen>
      cluster-1::> security key-manager onboard sync
    </screen>
  </example>
</command>
