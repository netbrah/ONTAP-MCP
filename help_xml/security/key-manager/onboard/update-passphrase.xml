<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/keymanager/onboard/keymanager_update_passphrase.smf
    table name: keymanager_update_passphrase
-->
<command>
  <name>security key-manager onboard update-passphrase</name>
  <description>This command provides a way to update the cluster-wide
  passphrase that is used for the Onboard Key Manager and initially created by
  running the <cmdname>security key-manager onboard enable</cmdname> command.
  This command prompts for the existing passphrase, and if that passphrase is
  correct then the command prompts for a new passphrase.
  When the Onboard Key Manager is enabled for the admin Vserver, run the
  <cmdname>security key-manager onboard show-backup</cmdname> command after
  updating the passphrase and save the output for emergency recovery scenarios.
  When the <cmdname>security key-manager onboard update-passphrase</cmdname>
  command is executed in a MetroCluster configuration, then run the
  <cmdname>security key-manager onboard sync</cmdname> command with the new
  passphrase on the partner site before proceeding with any key-manager
  operations. This allows the updated passphrase to be replicated to the
  partner site.
  </description>
  <parameters>
    <parameter>
      <name>vserver</name>
      <description>Use this parameter to specify the Vserver for which the
      the Onboard Key Manager passphrase is to be updated.</description>
    </parameter>
    <parameter>
      <name>existing-passphrase</name>
      <hide/>
    </parameter>
    <parameter>
      <name>new-passphrase</name>
      <hide/>
    </parameter>
    <parameter>
      <name>force</name>
        <description>Set this parameter to true to bypass the request for and validation of the existing passphrase.</description>
    </parameter>
  </parameters>
  <example>
        <p>The following example updates the cluster-wide passphrase used for
        the Onboard Key Manager:</p>
        <screen>
cluster-1::*> security key-manager onboard update-passphrase

Warning: This command will reconfigure the cluster passphrase for onboard
         key management.
Do you want to continue? {y|n}: y

Enter current passphrase:

Enter new passphrase:

Reenter the new passphrase:
Update passphrase has completed. Save the new encrypted configuration data in
a safe location so that you can use it if you need to perform a manual recovery
operation. To view the data, use the "security key-manager onboard show-backup"
command.
        </screen>
    </example>
</command>
