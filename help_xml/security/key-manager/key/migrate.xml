<?xml version="1.0" encoding="UTF-8" ?>
<!--
    smf file: src/tables/keymanager/key/keymanager_key_migrate.smf
    table name: keymanager_key_migrate
-->
<command>
    <name>security key-manager key migrate</name>
    <description>This command provides a mechanism to migrate the existing keys
    of a data Vserver from the admin Vserver's key manager to their own key
    manager or vice versa. The keys stay the same and the data is not rekeyed,
    only the keys are migrated from one Vserver's key manager to another. After
    a successful migration to the new key manager, the data Vserver keys are
    deleted from the previous key manager.

    </description>
    <parameters>
        <parameter>
            <name>from-vserver</name>
            <description>Use this parameter to specify the name of the Vserver
            whose key manager the keys are migrated from.</description>
        </parameter>
        <parameter>
            <name>to-vserver</name>
            <description>Use this parameter to specify the name of the Vserver
            whose key manager the keys are migrated to.</description>
        </parameter>
	</parameters>
    <example>
        <p>The following example migrates the keys of "datavs" data Vserver
        from "cluster-1" admin Vserver's key manager to "datavs" data Vserver's
        key manager:</p>
        <screen>
cluster-1::> security key-manager key migrate -from-vserver cluster-1 -to-vserver datavs
        </screen>
        <p>The following example migrates the keys of "datavs" data Vserver
        from "datavs" data Vserver's key manager to "cluster-1" admin Vserver's
        key manager:</p>
        <screen>
cluster-1::> security key-manager key migrate -from-vserver datavs -to-vserver cluster-1
        </screen>
</example>
</command>
